- name : install and start nginx
  hosts : frontend
  become : yes  #to run tasks with sudo privileges means root user
  tasks :
  - name: Install Nginx package
    ansible.builtin.package:
      name: nginx
      state: latest

  - name: start and enable nginx service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
