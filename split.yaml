- name: split function
  hosts: frontend
  become: yes
  vars:
   My_name: Anil Kumar ,Anil , Akula, Akula anil royals, Royals
  tasks:
  - name: using split function
    ansible.builtin.debug:
      msg: "Name: {{My_name | split(',')')}}" 